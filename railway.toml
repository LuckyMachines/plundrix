[build]
builder = "nixpacks"
buildCommand = "npm --prefix app ci && npm --prefix app run build"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "cd app && node scripts/serve-dist.mjs"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
