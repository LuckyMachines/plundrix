# Transparent part masters

Accepted 1024x1024 transparent PNG masters live here using the exact filenames declared in `app/art/assets.mjs`. Preserve alpha and the complete object silhouette.

Generate into versioned candidate space, complete the prompt pack's manual checks, promote with `npm run art:promote`, then run `npm run art:build -- --asset <id>` to create the delivery derivative.
